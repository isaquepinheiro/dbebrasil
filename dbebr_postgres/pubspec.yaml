name: dbebr_postgres
description: DBEBr Framwork for Dart/Flutter (PostgreSQL - package postgres)
version: 0.0.1
repository: https://github.com/isaquepinheiro
publish_to: 'none'

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  dbebr_core:
    path: ../dbebr_core
  postgres: ^2.6.1
  meta: ^1.9.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
